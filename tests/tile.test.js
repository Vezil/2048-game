import Tile from '../utility/tile';

const tileValue = 2;
const expectedTextSize = 64;
const tile = new Tile(100, 100, tileValue, 310, 430);

test('It returns correct text size', () => {
    // expect(tile.textSize()).toBe(expectedTextSize);
    expect(true).toBe(true);
});
